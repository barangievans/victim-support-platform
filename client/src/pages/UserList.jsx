// src/pages/UserList.jsx
import React from 'react';

const UserList = () => {
    return (
        <div>
            <h1>User List</h1>
            {/* Logic to display the list of users and a form to add a new user */}
        </div>
    );
};

export default UserList;
